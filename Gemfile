source :rubygems
gemspec

gem "rake"

# for building opal.js
group :building do
  gem "sprockets"
end

# for rebuilding grammar.rb from grammar.y
group :grammar do
  gem "racc"
end

# running tests on command line
group :testing do
  gem 'opal-spec'
end
